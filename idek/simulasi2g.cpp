#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
int pair<int, pair<int,int>>a;
int main(){
	int n,k;
	cin>>n>>k;
	for(int i=0;i<n;i++){
		cin>>a.first>>a.second.first>>a.second.second;
	}
	sort(a,a+n);
	
}
